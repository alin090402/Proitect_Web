import { ActivityDirective } from './activity.directive';

describe('ActivityDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivityDirective();
    expect(directive).toBeTruthy();
  });
});
